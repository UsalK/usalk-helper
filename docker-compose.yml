services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    volumes:
      - ./storage:/storage
      - ./backend/db:/app/db

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
